{
  "name": "enertalk-alwayson-calculator",
  "version": "0.2.2",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "enertalk-api-client": "^0.6.2",
    "moment-timezone": "^0.5.14"
  },
  "devDependencies": {
    "axios": "^0.17.0",
    "husky": "^0.14.3",
    "jest": "^21.2.1"
  },
  "scripts": {
    "test": "jest --watch",
    "calculate": "node integration_test",
    "prepush": "node scripts/clear_credentials && git add . && git commit --amend --no-edit"
  }
}
